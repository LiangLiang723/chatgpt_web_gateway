# V0.1.2 OpenAI-Compatible Agent Compatibility Plan

**Governing design:** [`../specs/2026-08-31-v0.1.2-openai-agent-compat-design.md`](../specs/2026-08-31-v0.1.2-openai-agent-compat-design.md)

**Status:** completed maintenance implementation; final repository verification and main integration recorded in Project State/CHANGELOG.

## Tasks

- [x] Reproduce Cherry Studio `reasoning_content` Assistant-history rejection with strict request-schema coverage.
- [x] Add explicit Chat Completions compatibility fields for Cherry Studio, Pi, OpenClaw, and Hermes without enabling arbitrary additional properties.
- [x] Capture the installed Codex CLI Responses request shape and cover current Responses metadata/function/namespace/custom/server-tool declarations.
- [x] Bridge Responses namespace and custom/freeform tools into the existing external-function protocol and restore their public Responses output shapes.
- [x] Add HTTP-level regressions proving Cherry/agent Chat Completions and Codex Responses shapes reach the normalized streaming boundary instead of failing with HTTP 400.
- [x] Add Cherry-compatible model metadata aliases plus configurable context/max-input/max-output token hints.
- [x] Keep Claude Code/Anthropic Messages explicitly out of scope per user decision.
- [x] Synchronize README, API compatibility, architecture, testing, deployment config, Project State, CHANGELOG, and V0.1.2 version metadata.
- [ ] Run final fresh `corepack pnpm verify`, `git diff --check`, staged safety review, commit, fast-forward into `main`, re-verify merged `main`, and push.

## Evidence before final verification

- Focused compatibility suite: **8 files / 56 tests** passed after custom-tool bridge completion.
- Additional current-Codex schema coverage for `output_schema`, strict `tool_search`, and current `web_search` fields passed together with TypeScript typecheck.
- Full maintenance-candidate `corepack pnpm verify` passed twice at **86 test files / 610 tests**, with format/lint/typecheck/build/Project Memory/Docs/Architecture/Version all green; `git diff --check` also passed before Git integration.
- No Docker build, Browser request, or authenticated ChatGPT E2E has been run for this adapter-only maintenance candidate.
