# Specs & Plans 工作流

- `specs/`：设计和决策，回答“为什么这样做、边界是什么、成功标准是什么”。
- `plans/`：实施步骤，回答“具体改哪些文件、怎么测试、当前执行到哪”。

复杂工作顺序：

```text
理解现状
→ 设计方案
→ 用户批准设计
→ 写 spec
→ spec 自检
→ 写 plan
→ 按 plan 执行
→ 持续更新 plan 状态
→ 项目记忆回写
```

已经被新设计替代的计划不要删除历史；在计划中标记 `[-]` 并说明替代文件。
